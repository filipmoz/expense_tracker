# basic – easy XLS generation

Run from this directory:

```bash
python generate.py     # writes output.xlsx
python xml.py          # writes output_openpyxl.xlsx
```

No install needed beyond project deps (xlsxwriter, openpyxl).
